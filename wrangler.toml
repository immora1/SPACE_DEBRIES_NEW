name = "space-debries"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

# STORY_DB is intentionally configured as a Pages binding in the Cloudflare
# dashboard for both Production and Preview. Never add a placeholder UUID here:
# Wrangler treats this file as deploy configuration and rejects invalid IDs.
